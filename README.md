# Simple Calculator

Django calculator project.

## Run locally

```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

## Deploy on Render

This repository includes `build.sh` and `render.yaml` for deployment.
